using UnityEngine;
using TMPro;
public class LocalizationDirector : MonoBehaviour
{
    [SerializeField] private object[] Texts = new object[] { 1, "string", 1.2f, 2 };
    private void Awake()
    {
        
    }
}
